torchgan.datasets
=================

.. currentmodule:: torchgan.datasets

This subpackage provides thin wrappers around :mod:`torchvision.datasets`. It allows
users to directly get the dataset in the format that is needed by the :mod:`torchgan.trainer`.

The current datasets available in this format are:

.. contents::
    :local:
